server:
  port: 8082

spring:
  application:
    name: product-service  # ??????????????????

  datasource:
    url: jdbc:mysql://localhost:3306/spring_cloud_api_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: abc123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ????
    show-sql: true      # ?? SQL
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  # Eureka Server ???
  instance:
    prefer-ip-address: true